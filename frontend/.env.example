# Copy to .env.local for local overrides (optional).
# Production: set VITE_API_URL in your host's environment before building.

# Hosted Node API (must include /api suffix)
VITE_API_URL=http://localhost:5000/api

# Optional: Python PDF converter (Flask on port 5001 locally)
# VITE_PDF_CONVERTER_URL=https://your-pdf-service.example.com
