PORT=5000
MONGO_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/
MONGO_DB_NAME=test
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=
FRONTEND_URL=http://localhost:5173
APP_NAME=Scholarship Records Management System
SMTP_SERVICE=gmail
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Controller admin dashboard (optional — heartbeats disabled if unset)
CONTROLLER_URL=http://localhost:5000
# Copy from Controller dashboard, or set the same value when registering this project there
PROJECT_API_KEY=
# APP_VERSION=0.1.0-beta.1
